import React from 'react';
import ReactDOM from 'react-dom/client';
import ParticipantFeedbackGenerator from './ParticipantFeedbackGenerator';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ParticipantFeedbackGenerator />
  </React.StrictMode>,
);
